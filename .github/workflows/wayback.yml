name: Archive Website

on:
  push:
    branches: [main]

  schedule:
    - cron: '0 0 * * *'

  workflow_dispatch:

jobs:
  archive:
    runs-on: ubuntu-latest

    steps:
      - run: curl -L -X POST "https://web.archive.org/save/https://zozimustechnologies.github.io/"